Nightglass runtime assets

Validated notification sounds live under sounds/<catalog-id>.wav.
Accepted catalog IDs are notification, call, calendar, email, alarm, timer,
success, and warning. WAV files must be PCM16 mono at 16 kHz and no longer
than five seconds. The firmware never formats this volume automatically.
